# 03. デザインシステムのブラッシュアップ(共有ページのパイロット改装)

- Status: planned
- Issue: #115
- PR: -

---

## 1. 背景・目的

現行UIは見た目が「MUI標準そのまま」に寄っており、`02_design_system/00_basic_design.md` が掲げる
コンセプト(「和の競技にふさわしい、落ち着いた品のある見た目」/ 深緑 `#1B5E43`)が画面に表れていない。

実態は `frontend/src/theme/index.ts` が示している。102行のうち大半がトークン定義で、
`components` の styleOverrides は `MuiButton`(textTransform / disableElevation)と
`MuiCssBaseline`(scrollbarGutter)の2つだけ。**残りはすべてMUIの既定値**である。

これはファーストリリース時に「低コスト・シンプル・既存コンポーネントライブラリに準拠」を
優先した判断の結果であり、コンセプト自体が誤っているわけではない。したがって本計画は
**新しいデザイン方向性を発明せず、既存コンセプトを画面に表出させる**ことを目的とする。

完了時に得られる状態:

- 参加者が最も多く触れる共有ページ(S10)・結果入力(S11)が、コンセプトを体現した見た目になっている
- デザイントークンが刷新され、`01_design_principles.md` と `theme/index.ts` が同期している
- `frontend-design` / `baseline-ui` skill がリポジトリに入り、以後のUI実装で継続的に使える
- #112 で獲得したアクセシビリティ水準が維持され、**回帰を機械検査で防げる**状態になっている

**最重要原則: 正確性 > シンプルさ > コスト > 機能 > 速度**

意匠の都合で正確性(結果の読み違い防止)を落とさない。特に勝敗表示・順位表示は、
見た目を変えても「誤読しにくさ」が現状より悪化してはならない。

## 2. 画面シナリオ

**本計画は挙動を変更しない。** 画面シナリオは現行のままであり、以下は
「改装後も一字一句そのまま成立していること」を確認する回帰対象として列挙する。

- 参加者が 共有URLをスマホで開くと 大会名・状態・現在ラウンドと組み合わせタブが表示される
- 参加者が 組み合わせタブで 自分の卓を探すと 卓番号・対戦相手・結果・申告状態が読み取れる
- 参加者が 対局カードの「結果入力」を押すと 結果入力画面へ遷移する
- 参加者が 結果入力画面で 自分がどちらかを選び 勝敗を選ぶと 確認ダイアログが出て、申告すると組み合わせタブへ戻る
- 参加者が 申告済みの対局を再度開くと 誰が何を申告したかが表示される
- 参加者が 申告不一致の対局を開くと 警告(warning)が表示され、運営者への連絡が案内される
- 参加者が 順位表タブを開くと(ラウンド1確定後)トップ3のメダルカードと4位以降のリストが表示される
- 参加者が ラウンド1確定前に順位表タブを開くと 空状態が表示される
- 参加者が 対戦結果タブを開くと 戦績一覧表が表示される
- 参加者が 無効・失効した共有URLを開くと 「このURLは無効です」が表示される
- 参加者が 結果入力が締め切られた対局を開くと 締切の案内が表示され入力できない

観点チェック:

- 運営者(PC)側の画面は**構造を変更しない**。ただしトークン刷新の影響は受ける(§4)
- 個人戦のみをパイロット対象とする。団体戦(`TeamSharedPage` / `SharedTeamResultPage`)は構造改装の対象外
- グループ分けあり/なしの分岐(`multiGroup`)は現行どおり維持する

## 3. UI仕様

### 3-1. 本セクションの位置づけ(通常の機能追加との違い)

通常の機能追加ではここでレイアウトと状態を文章で確定させる。しかし本計画は
**具体的な意匠を探索して決めること自体がタスク**であり、計画段階で色値やレイアウトを
確定させることはできない。

そこで本セクションでは以下を確定させる。**これが実装PRの制約条件になる。**

1. 動かしてはいけない不変条件(§3-2)
2. 動かしてよい範囲(§3-3)
3. 探索の進め方と go/no-go の判断基準(§3-4)
4. 改装対象画面の現行構造と、変更してよい/いけない要素(§3-5)

具体的なトークン値・レイアウトは探索ステップの成果物として決定し、
**実装に入る前に `01_design_principles.md` へ記載する**(CLAUDE.md「新しいデザイントークンは
対応ドキュメントに追記してから使う」を満たすため)。

### 3-2. 不変条件(意匠の都合で動かさない)

利用者層(20代〜70代)と競技特性(結果を誤読できない)に由来するため、
デザイン方向性がどう決まっても動かさない。

| 不変条件 | 出典 |
|---|---|
| デザイン3原則(明快・堅実・信頼) | `00_basic_design.md` §1 |
| 画面の設計優先度(共有ページ > 結果入力 > 順位表 > 運営者管理画面) | `00_basic_design.md` §4 |
| タップターゲット最小44x44px / 結果入力ボタン56px以上 | `01_design_principles.md` §3 |
| 共有ページ本文の最低16px(高齢参加者配慮 + iOS自動ズーム防止) | `01_design_principles.md` §2 |
| テキストと背景のコントラスト比 WCAG AA(4.5:1)以上 | `01_design_principles.md` §1 |
| 勝敗を色だけに頼らない(○/● 記号を必ず併記) | `01_design_principles.md` §1 |
| 順位表の並び替えアニメーション禁止(順位が動いて見えると誤読リスク) | `03_animation_system.md` §4 |
| 数字の等幅表示(`tnum`。順位表の桁揃え) | `01_design_principles.md` §2 |
| ダークモード非対応 | `01_design_principles.md` §4 |

### 3-3. 動かしてよい範囲

ファーストリリース時の低コスト判断が出どころであり、今回見直してよい。

- **カラーパレットの具体値**(Issue #115 の決定: パレットごと刷新する)
- タイポグラフィスケール(variant ごとのサイズ・ウェイト・行間・字間)
- `elevation 0〜2 のみ` / `角丸8px 一律` という制約そのもの
- 情報密度・余白の取り方、罫線と面の使い分け
- `00_basic_design.md` §4 の「運営者管理画面は機能性優先でMUI標準に寄せてよい」という逃げ道
- `theme.components` の styleOverrides を増やすこと(現在ほぼ空)

### 3-4. 探索の進め方と go/no-go

```
① skill追加 + 不変条件の確定(本計画のマージで完了)
    ↓
② frontend-design で共有ページの意匠を探索(Storybook上。既存 SharedPage.stories.tsx を使う)
    ↓
③ 実機(スマホ実寸)で確認 → go / no-go を人間が判断   ← ここが唯一のゲート
    ↓ go
④ トークン確定 → 01_design_principles.md 更新 → theme/index.ts 実装
    ↓
⑤ 共有ページ・結果入力の改装 → 全画面の追従確認
    ↓
⑥ fixing-accessibility 再実行 → baseline-ui でチェック
    ↓
⑦ VRTベースライン更新
```

**go/no-go の判断基準**(③で人間が見るもの):

- 3原則(明快・堅実・信頼)が現行より強まっているか。特に「一目でわかる・押し間違えない」が後退していないか
- コンセプト(和・落ち着いた品)が言われなくても伝わるか
- 会場の環境(明るい照明・急いでいる・スマホを片手持ち)で読めるか
- 不変条件(§3-2)を1つも破っていないか

no-go の場合は②に戻る。②〜③はコードをmainに入れないため、破棄コストはStorybook上の作業のみ。

### 3-5. 改装対象画面の現行構造

#### S10 共有ページ(`SharedPage.tsx` — 個人戦)

現行のレイアウト構成(上から順):

```
Container(maxWidth="md", py:3)
├─ 大会名(h2/h1) + StatusBadge         ← 横並び・flexWrap
├─ 「第Nラウンド / 全Mラウンド」(body2, text.secondary)
├─ Tabs(fullWidth): 組み合わせ / 順位表 / 対戦結果
└─ タブ本体
   ├─ 組み合わせ: [ラウンド選択(2R以降)] + 案内文 + 対局カード群(グループ見出しあり)
   ├─ 順位表:     RankingBoard(グループごと)         ← 共用コンポーネント
   └─ 対戦結果:   MatchResultsTable(グループごと)     ← 共用コンポーネント
```

対局カード(`SharedMatchCard`。このファイル内のローカルコンポーネント)の現行構造:

```
Card(variant="outlined")
└─ CardContent(flex, gap:2)
   ├─ 卓番号(h3, flexShrink:0)
   ├─ 中央ブロック(flexGrow:1, minWidth:0)
   │  ├─ 対局者1(body1・結果記号を前置)
   │  ├─ 対局者2(body1・結果記号を前置)
   │  └─ 状態テキスト(body2・不一致時のみ warning.main)
   └─ [結果入力ボタン](未入力時 contained / 入力済み outlined, size="small")
```

- **改装してよい**: ヘッダー部、Tabs、`SharedMatchCard`、空状態の見せ方、グループ見出し
- **構造を変えない**: `RankingBoard` / `MatchResultsTable`(§4-2の理由。トークン追従のみ)

#### S11 結果入力(`SharedResultPage.tsx` — 個人戦)

```
Container(maxWidth="sm", py:4)
├─ 「組み合わせへ戻る」(text button + ArrowBackIcon)
├─ Card(variant="outlined")
│  └─ CardContent
│     ├─ 「第Nラウンド・X卓」(body2, text.secondary)
│     ├─ 「A vs B」(h2/h1)
│     ├─ ReportStatus(確定結果 / 双方の申告内容 / Alert: info・warning)
│     └─ 状態別の本体
│        ├─ 締切時:   案内文のみ
│        ├─ side未選択: 「あなたはどちらですか?」+ 対局者2名のボタン(outlined, size="large")
│        └─ side選択済: 「あなたの結果を選んでください」+ 勝ち/負け/引き分け/両者負け + 「選び直す」
└─ ConfirmDialog(申告の最終確認)
```

- **改装してよい**: カード全体の見せ方、選択ボタンの意匠、ReportStatus の情報階層、Alert の表現
- **構造を変えない**: 2段階選択(どちらか → 勝敗)+ ConfirmDialog という誤タップ防止フロー。
  これは `02_component_design.md` §1 の ResultButton 規定であり、意匠ではなく安全機構である

### 3-6. 4状態・レスポンシブ・大量データ

いずれも**現行の挙動を維持する**。改装で失わないことを確認する対象として明示する。

| 状態 | 共有ページの表示(現行・維持) |
|---|---|
| 通常 | タブ切替で 組み合わせ / 順位表 / 対戦結果 |
| 空(0件) | `EmptyState`。大会開始前・組み合わせ未生成・順位未確定・戦績なしでメッセージを出し分け |
| ローディング | `FullPageSpinner` |
| エラー | `ErrorState`(再試行ボタン付き)。無効トークンのみ専用画面 |

- **レスポンシブ**: 375px を主対象とする(参加者はスマホ)。共有ページは `maxWidth="md"`、
  結果入力は `maxWidth="sm"`。デスクトップでは中央寄せの1カラムのまま。この方針は変えない
- **大量データ時**: 300人・全ラウンドで、順位表は4位以降のリストが縦に伸び、
  戦績一覧表は横スクロールする。改装で横スクロールの成立条件を壊さないこと
  (テーブルの最小幅・セル余白を増やす場合は 375px での確認が必須)

## 4. 技術設計

### 4-1. レイヤーごとの変更点

- **domain / application / infrastructure**: 変更なし
- **presentation(backend)**: 変更なし
- **API**: 変更なし(`schema/openapi.yaml` は更新しない)
- **frontend**: 以下のみ

### 4-2. トークン刷新の波及範囲(パレット全面刷新のため広い)

Issue #115 の決定「トークンは全画面に効かせ、コンポーネント改装は共有ページのみ」に従う。
`src/` 配下のハードコード色は **0件**でスタイルがすべてテーマ経由のため、
トークン差し替えは全画面へ自動伝播する。裏を返すと**確認範囲は全画面**になる。

| 影響先 | 内容 | 対応 |
|---|---|---|
| 共有ページ・結果入力 | 改装対象 | §3-5 のとおり再設計 |
| 運営者管理画面(全ページ) | トークン追従のみ | 構造は変えない。表示崩れ・コントラスト低下がないか確認 |
| `RankingBoard` / `TeamRankingBoard` | `rank.gold/silver/bronze` を参照 | **新パレットとの再調和が必要**。メダル色が浮かないか確認 |
| `MatchResultsTable` / `PairingTable` / `ParticipantTable` | ヘッダーが `primary.main` + 白文字、ゼブラが `background.paper`/`default` | primary 変更時に**白文字のコントラストAA割れが起きないか必ず確認** |
| 印刷帳票(`components/features/print/` 9ファイル) | `theme.print` + `text.primary` / `divider` を参照 | モノクロ運用(`headerBg` 薄グレー・緑ベタ塗り禁止)を維持。手書き罫線が薄くならないこと |
| `StatusBadge` | success / warning / info / default の Chip | セマンティックカラー変更時に判別性を確認 |

**特に注意**: `primary.main` は「表ヘッダーの白文字背景」と「主要ボタンの背景」を兼ねている。
明度を上げると白文字が AA を割り、下げるとボタンが重くなる。ここが今回のパレット刷新で
最も制約が強い点であり、探索段階で先に決めるべき値である。

### 4-3. 変更するファイル

- `frontend/src/theme/index.ts` — パレット刷新、タイポグラフィスケール、`components` styleOverrides の追加
- `frontend/src/pages/SharedPage.tsx` — ヘッダー・Tabs・`SharedMatchCard` の改装
- `frontend/src/pages/SharedResultPage.tsx` — カード・選択ボタン・ReportStatus の改装
- 必要に応じて `frontend/src/components/ui/` の既存コンポーネント(`EmptyState` / `StatusBadge` 等)
- `frontend/tests/vrt/__screenshots__/` — ベースライン更新(workflow_dispatch で実行)

新しい抽象は原則作らない。共有ページ固有の意匠がローカルコンポーネントで収まらない規模になった
場合のみ `components/ui/` への切り出しを検討し、そのときは `02_component_design.md` に定義を先に追加する。

### 4-4. skill の追加

`.claude/skills/` 配下に置き、リポジトリで共有・バージョン管理する(`fixing-accessibility` と同じ運用)。

| skill | 出所 | 用途 |
|---|---|---|
| `frontend-design` | Anthropic公式 `anthropics/skills` | ②の意匠探索 |
| `baseline-ui` | `ibelick/ui-skills` | ⑥の仕上げチェック(**自動適用しない**) |

`baseline-ui` を自動適用しない理由: 汎用の「スペーシング・タイポを整える」パスであり、
本計画で意図的に決めたデザイン選択を「不揃い」と見なして潰す可能性がある。指摘を読んで
人間が採否を判断する。

## 5. 受け入れケース

Prefix は `SHR`(共有)。既存最大 `SHR-AC-017` の次から採番する。

本計画は挙動を変えないため、新規ケースは**改装で壊れやすい不変条件を機械検査に落とすもの**に限る。
これは #112 のアクセシビリティ成果を回帰から守り、「うまくいかなければ切り戻す」判断を
安全にするためのガードでもある。いずれもテーマトークンに対する純粋な検証で、
レイアウト計測に依存しない(jsdom では実レイアウトを測れないため)。

| ID | P | 受け入れ基準 | 検証手段 |
|---|---|---|---|
| SHR-AC-018 | P1 | 共有ページ・結果入力ページの本文タイポグラフィ(body1)が16px以上である | Vitest |
| SHR-AC-019 | P1 | テーマで定義した文字色と背景色の主要な組み合わせがWCAG AA(4.5:1)以上のコントラスト比を満たす | Vitest |
| SHR-AC-020 | P2 | 共有ページの対局カードは勝敗を色だけでなく記号(○/●)で併記する | Vitest |

- SHR-AC-019 の「主要な組み合わせ」は最低限次を含む:
  `text.primary`/`background.default`、`text.primary`/`background.paper`、
  `text.secondary`/`background.default`、`primary.contrastText`/`primary.main`(表ヘッダー)、
  `error.main`・`success.main`・`warning.main` の各文字色/`background.paper`
- 優先度は `02_severity.md` に従い P1(仕様違反・ユーザー影響のあるバグ。大会当日に運営が
  止まる・結果が狂う・情報が漏れるには当たらないため P0 ではない)。
  SHR-AC-020 は既存実装が満たしており回帰検知が目的のため P2

## 6. 更新する設計資料

- [x] `.claude/07_plans/03_design_system_brushup.md` — 本計画(このPR)
- [x] `.claude/06_adr/06_design_system_direction.md` — 方針転換のADR(このPR)
- [x] `.claude/05_acceptance/01_acceptance_scope.md` — SHR-AC-018〜020 を Status=todo で追加(このPR)
- [ ] `.claude/02_design_system/01_design_principles.md` — **実装PR**。新パレット・タイポスケール・
      角丸/影の方針。`theme/index.ts` と同期させる
- [ ] `.claude/02_design_system/00_basic_design.md` — **実装PR**。§4 の「運営者管理画面はMUI標準に
      寄せてよい」の扱いを決めた結果を反映
- [ ] `.claude/02_design_system/02_component_design.md` — **実装PR**。共有ページの対局カード・
      結果入力ボタンの意匠を変更した場合の記述更新
- [ ] `.claude/02_design_system/03_animation_system.md` — **実装PR**。トランジションを増やす場合のみ
- [ ] `.claude/02_design_system/04_layout_system.md` — **実装PR**。余白・グリッドの基準を変える場合のみ

`schema/openapi.yaml` と `05_swiss_pairing_algorithm.md` は**更新しない**(API・マッチング・順位計算に変更なし)。

## 7. DoD(完了の定義)

- [ ] `pnpm run check`(frontend)が通る。backend に変更がないため `./gradlew check` は対象外
- [ ] SHR-AC-018〜020 が台帳で done になり、対応するテストにIDが埋まっている
- [ ] 新規画面はないため smoke E2E の新設は不要。既存の共有ページ E2E がグリーンであること
- [ ] §6 で挙げた設計資料が実装PRで更新されている(特に `01_design_principles.md` と
      `theme/index.ts` の値が一致していること)
- [ ] ローカル実機で動作確認済み(`.claude/skills/verify`)。**375px の実機スマホで確認する**
- [ ] `fixing-accessibility` skill を再実行し、コントラスト・フォーカス可視・タップ領域の
      回帰がないことを確認した
- [ ] `baseline-ui` skill を実行し、指摘の採否を判断した(全採用しなくてよい)
- [ ] `vrt.yml` を手動実行してベースラインを更新した。**差分が共有ページ以外にも出るため、
      全スクリーンショットの差分を目視で確認してから更新する**(意図しない崩れの検出機会)
- [ ] 印刷帳票をブラウザの印刷プレビューで確認した(モノクロ運用・手書き罫線の視認性)

## 8. リスク・未確定事項

| リスク | 対応 |
|---|---|
| **`primary.main` の制約が厳しい**(表ヘッダーの白文字AAと主要ボタンの重さを両立する必要) | 探索段階で最初に決める。決まらなければヘッダーの文字色を白から変える案も検討する |
| **パレット刷新が印刷帳票のインク消費・視認性を悪化させる** | 印刷はモノクロ運用を維持する制約を先に固定済み(§4-2)。印刷プレビュー確認をDoDに含めた |
| **順位カラー(金銀銅)が新パレットから浮く** | メダル色も再調和の対象に含める(§4-2)。ただし金銀銅の識別性は落とさない |
| **VRT差分が全画面に出て、意図しない崩れが埋もれる** | ベースライン更新前に全差分を目視確認することをDoDに明記した |
| **意匠を優先して誤読しやすくなる**(最重要原則違反) | 不変条件(§3-2)を実装PRの制約として固定。go/no-go の判断基準に「押し間違えない」を明記 |
| **探索が発散して決まらない** | ③の go/no-go で判断する。2回 no-go が続いたら「控えめ(小幅)」へ方針を落とす |

**中止・切り戻し条件**: ③の go/no-go で no-go が2回続いた場合、パレット全面刷新を諦め、
余白・タイポ階層の調整に留める(Issue #115 の選択肢「控えめ(小幅)」に相当)。
マージ後に問題が発覚した場合は、トークン差し替えのコミットを revert すれば全画面が現行に戻る
(ハードコード色が0件のため成立する)。

**未確定事項**(実装PRで決める):

- 新パレットの具体値、タイポグラフィスケール
- `elevation 0〜2` 制約・`角丸8px` 一律を維持するか
- `00_basic_design.md` §4 の「運営者管理画面はMUI標準に寄せてよい」を残すか削除するか
- 共有ページ固有の意匠を `components/ui/` へ切り出す必要が出るか
