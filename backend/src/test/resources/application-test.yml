# テスト用プロファイル(DynamoDB Local: 03_dynamodb_local_testing.md)
app:
  dynamodb:
    table-name: swiss-stage
    endpoint: ${DYNAMODB_ENDPOINT:http://localhost:8000}
  auth:
    jwt-secret: test-only-jwt-secret-0123456789abcdef
    secure-cookie: false
